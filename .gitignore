node_modules/
dist/
*.tgz
.env
.DS_Store
coverage/
.npm-cache/
.aceclaw/
.lint-cache.json
.agent-wiki.yaml
# #26 phase 2 — project-local override for COBOL system-call whitelist etc.
# Site-specific runtime program names go here, never committed.
.agent-wiki.local.yaml
.tmp-lint-bench/

# Local agent / IDE workspace dirs — never commit. PR #32 (force-pushed) leaked
# these via `git add -A`; orphan SHA stayed publicly reachable on GitHub. Always
# stage files explicitly; this list is the second line of defense.
.agents/
.claude/
.cortex/
skills-lock.json
docs/diagrams/*.drawio
docs/diagrams/*.png
